# Copy to .env next to this file. Every value is optional.

# API keys (widen gimmesubs' passive phase)
OTX_KEY=
# only queried when set
LEAKIX_KEY=
DNSDUMPSTER_KEY=
FULLHUNT_KEY=
# subfinder providers: see config/subfinder-provider-config.yaml.example

# Seconds per passive source. Raise on a slow link.
# PASSIVE_TIMEOUT=180
